// const toggleLoader = (dom) => {
//     let e = dom.nextElementSibling;
//     e.classList.toggle("hide");
// };

// module.exports = {
//     toggleLoader,
// };

window.showLoader = (dom) => {
    // let e = dom.nextElementSibling;
    // e.classList.remove("hide");
};

window.hideLoader = (dom) => {};

window.checkUsername = async (dom) => {};
